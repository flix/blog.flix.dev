public
# Fetched by `make` from flix/textmate.
syntaxes/flix.tmLanguage.json
# The syntax highlighting colours, written by Zola on every build from the theme
# named in config.toml. Linked by templates/tabi/extend_head.html.
static/giallo.css
